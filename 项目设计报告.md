# 自平衡瞄准小车设计报告

## 一、项目概述

本项目设计并实现了一台两轮自平衡瞄准小车，目标是在直径约 80 cm 的圆形黑线赛道上稳定直立行进，绕行一周乃至两周，同时让车载 405 nm 激光始终照射在圆心矿泉水瓶的感光纸上，并把激光点的上下波动控制在 2 cm 以内。赛题对系统提出了三类相互耦合的硬约束：车体必须靠两轮闭环"站得住、走得动"，视觉必须实时"看得准、对得上靶"，二者还要在毫秒级时序上协同而不互相拖累。

为此我们采用**双控制器异构架构**：TI MSPM0G3507（Cortex-M0+，无浮点单元）作为平衡车主控，独占高频时片专注姿态解算、电机闭环与赛道流程总指挥；K230（CanMV）作为视觉与执行控制器，负责双摄采集、循线解算、激光瞄准与云台双舵机直驱。两者通过一路带 CRC 校验的双向串口链路通信，惯性数据则由九轴模块的发送线 Y 分直达双方，实现责任清晰、算力解耦的工程划分。项目的主要创新点在于：面向无 FPU 主控的全整数化多速率级联平衡架构、自立与运动两套增益的赛道状态机、由主控相位闸门统一调度视觉的"软同步"协同机制，以及一套在低分辨率灰度图上即可稳定输出路径误差的轻量循线视觉流水线。

## 二、主控 MCU：平衡与运动控制核心

### 2.1 硬件平台与软件架构

主控选用 MSPM0G3507 LaunchPad，主频 80 MHz、Cortex-M0+ 内核**不带硬件浮点单元**，这一约束贯穿了整个软件设计。外设方面，电机经 TB6612 双 H 桥驱动，PWM 频率取 20 kHz 以避开人耳听觉与惯性测量通带；左轮编码器走片上唯一一路硬件正交解码（基于通用定时器的 QEI），右轮因器件只有一路硬件 QEI 而采用 GPIO 双边沿中断软件解码；姿态传感器替换为正点原子的串口惯性模块，板载扩展卡尔曼滤波直接以约 200 Hz 主动上报姿态帧，免去外部上拉电阻并卸载了主控算力；此外还包括电池分压采样、有源蜂鸣器、状态指示灯与一键启动按键。

软件采用**单任务轮询 + 系统节拍标志**的多速率调度，不引入实时操作系统。1 kHz 系统滴答驱动姿态解析与编码器更新，向上派生出 200 Hz、100 Hz、20 Hz、5 Hz、1 Hz 等子节拍，分别承载不同带宽的控制环、传感与日志任务。代码自底向上分为板级支持层（直接操作外设）、中间算法层（PID、协议、姿态解析）与应用层（平衡、安全、赛道状态机、蜂鸣曲谱）三层，引脚分配维护一份唯一真源文档，任何硬件改动都先改表再改代码，保证了长达数十次迭代中的可追溯性。

针对无 FPU 这一痛点，我们坚持"算的人就是用的人"的整数化原则：1 kHz 高频路径只做 16 位编码器计数差分扩展为 32 位、整型速度窗口累计，全程无浮点；角度、转速等浮点换算延迟到业务层按需触发。重写后高频更新路径的开销从约 0.5% CPU 降到不足 0.001%，为高带宽平衡环腾出了充裕的时间预算。

### 2.2 平衡控制系统架构（核心）

平衡控制是主控最具技术含量的部分，其架构经历了从单环到四级、再收敛为两级串级的完整演进，最终形态兼顾了响应速度、抗扰能力与可整定性。

**两级串级俯仰控制。** 当前架构对俯仰方向采用速度外环与角度环的两级串联：速度外环以 20 Hz 运行，输入是左右轮平均速度误差，输出一个目标倾角（限幅在 ±10° 内），其物理意义是"想前进就主动前倾一点"，从而消除贴地慢漂；角度环以 100 Hz 运行，输入是实际倾角与目标倾角之差，**直接输出 PWM 占空比**驱动电机。曾经我们尝试过更激进的"速度→角度→角速度→PWM"四级级联，但对照成熟方案并结合实测发现,独立的角速度内环带来的相位裕度收益有限,反而增加了整定维度与振荡风险,因此最终收敛为两级,把角速度信息以另一种方式融入角度环。

**陀螺直通的微分项。** 角度环的微分项不走标准 PID 库的"误差差分"路径，而是直接取惯性模块的俯仰角速率乘以微分增益。这是因为俯仰角在送入控制器前要经过一阶低通滤波以抑制单帧抖动，而低通会引入相位滞后，使常规微分项响应变钝；改用未经滤波的陀螺角速率后，微分项实现了近零延迟的阻尼，整定直觉也更直接——增益旋钮直接对应"角速度阻尼"。我们还在角速度测量上叠加了一个时间常数约 10 ms 的低通，专门抑制 20 kHz PWM 振动经车体结构耦合进惯性测量的高频噪声，避免它放大成电机抖动与发热。

**航向角环与差速闭环。** 横摆方向单独设一路航向角环（20 Hz），用以抑制两轮命令对称时仍会出现的原地偏航漂移。其数据源可在绝对偏航角与陀螺积分之间编译期切换，默认采用陀螺积分以规避电机磁干扰与 ±180° 跳变；航向角环的输出不再直接叠加为左右 PWM 差分，而是作为下一级**差速环**的目标速度差。这样设计的原因是：PWM 到实际速度差的映射受电池电压、地面摩擦、电机温度影响而时变严重，把航向修正落实到"速度差"这个可闭环量纲上，控制才稳定可控。最终形成"航向角环 → 差速环 → 左右轮 PWM"的三级链路，并在速度与差速的目标值上引入 EMA 低通，避免阶跃指令引起的"前倾启动—后仰回退"顿挫。

**控制器与死区处理。** 三个环统一使用自研的定点友好型 PID 控制器，具备按拍累加积分、微分先行（对测量量微分以减小设定点跳变冲击）、输出限幅与积分抗饱和、以及一个用于突破静摩擦的输出偏置项。电机静摩擦死区是平衡车在零点附近的顽疾——任何微小的命令都被死区吞掉，越过门槛又跳变成方波。早期我们用双门槛（区分静摩擦与动摩擦）四路分散补偿缓解，后来进一步引入 **sigma-delta 脉冲密度调制**：对低于死区的微弱命令不直接映射，而用累加器逐拍积分，累到阈值发射一次固定幅值脉冲，使子死区命令的长期平均输出依然线性。进入平衡模式后则索性关闭板级死区重映射，改由角度环的输出偏置直接突破静摩擦，让控制律对零点附近的微动拥有完全的线性权限。

所有 PID 增益默认为零（失效安全，上电不会自己乱动），通过串口或视觉端注入后才工作，配合自研的 PyQt 串口实时绘图工具与高频 CSV 数据流，整定效率大幅提升。

### 2.3 赛道状态机与自主起立

为完成"自立—循线—判圈—暂停—再循线—停车"的完整赛道流程，主控实现了一个总指挥状态机，把整圈任务拆解为自立摆起、稳定确认、循线、满圈减速、暂停五秒、末圈刹车、完成等阶段，由主控自主推进。

**自主起立的双增益设计**是其中的难点。直接把 30°~40° 初始倾角下的设定点设为 0°，会让角度环积分在不到一秒内饱和到极限，PWM 顶到满量程、电机疯转、编码器中断雪崩，进而干扰视觉端。我们为自立段设计了一套专用的"猛起 PID"：比例项调大以产生足够的起摆冲量，**积分项强制置零**以避免大误差下的积分饱和，微分项作为临近直立时的"减速—稳定"阻尼，是首要整定对象；摆稳判据为倾角与角速度同时进入阈值并持续约 400 ms。摆稳后再无缝切换到经过实测整定的"运动 PID"。判圈复用了偏航角积分与编码器里程的双判据，并以超时兜底。

考虑到冷上电时主控自检进入待命状态约需 5 秒、远早于视觉端约 10 秒的启动，过早起立会因编码器浮空噪声与姿态未收敛而疯冲，状态机加入了"等视觉在线再起立"的逻辑，并在启动时播放一段非阻塞的蜂鸣曲谱作为人机提示，验证了起立链路确已触发。

### 2.4 安全与可靠性设计

平衡车一旦失稳极易损坏硬件，可靠性是工程底线。主控实现了五态安全状态机（待命、就绪、低压告警、跌倒、低压急停），以明确的优先级管理：倾角超过 60° 即判跌倒，触发脉冲式刹车并断使能；电池电压跌破阈值分级降功率与急停，且低压急停不自动恢复，必须人工重新使能。为应对冷上电时电源缓启动导致的电压误判，电池采样改为基线窗口平台检测加超时兜底的预热自适应策略。此外，我们把分散的安全状态变量收编进单一结构体并在首尾插入校验魔数（canary），每次读取时校验，一旦因栈溢出被覆盖立即触发系统复位，配合硬件错误处理直接复位的策略，使"假死"变为可观察、可恢复的快速重启。

### 2.5 主控与视觉的通信协议与协同

主控与视觉的通信是双机协同的中枢，也是本项目的关键技术点之一。我们刻意把它设计为**两条物理独立的链路**：惯性模块的发送线经 Y 分同时接到主控与视觉的独立串口接收端，使视觉无需经主控转发即可直接拿到约 200 Hz 的原始六轴数据用于云台前馈，主控则省下一路 DMA 通道、对视觉的发送降到约 240 字节每秒的低频阻塞写；命令链路则是一路 115200 双向串口。

命令帧采用定长包头包尾加 CRC16-CCITT 的格式，校验范围覆盖长度、命令字与负载，低字节在前。主控以 20 Hz 上报车辆状态（平均速度、安全状态、电池电压，赛道模式下追加阶段号与圈号），1 Hz 发心跳；视觉以约 40 Hz 下发运动指令（纵向速度、角速度、模式），约 2.5 Hz 回心跳，并可按需注入 PID 增益或透传文本调试命令。任一端在 500 ms 内收不到对方帧即判离线并主动归零指令，主控随即退化为原地平衡告警，杜绝了空跑指令的安全隐患。

最能体现协同思想的是**赛道相位闸门**机制。赛道模式下主控是总指挥，视觉每帧照常计算路径误差，但"是否真正下发驱动"由主控上报的阶段号决定：只有处于循线阶段时视觉才下发非零运动指令，自立、刹车、暂停、完成等阶段一律输出零速度。这种"软同步"既保留了视觉控制律的完整性,又彻底避免了双端意图冲突导致的顿挫、误判圈与跌倒，把两个异构控制器的时序对齐问题用一个单字节的阶段号优雅地解决了。考虑到联调初期曾出现 CRC 查找表转录错误导致特定帧持续坏帧的问题，我们最终把查表改为按位计算，与视觉端 Python 实现严格等价，坏帧率随之归零；命令链路波特率也从 921600 降到 115200，把长杜邦线场景下的误码率从百分之几压到万分之一以下。

## 三、视觉处理 K230：感知与执行核心

### 3.1 双摄系统与软件分层

视觉端运行在 K230 的 CanMV（MicroPython）环境，使用两路摄像头：一路朝向地面做循线，一路装在云台上朝向瓶子做激光瞄准。循线采用同一传感器双通道方案——显示通道直接绑定到屏幕零拷贝输出，算法通道取 320×240 灰度图作为处理输入，显示与算法帧率解耦统计。

一个重要的实测发现是：CanMV 取帧接口存在约 30 帧每秒的实际上限，并非配置问题，而是软件层每次取帧固有约 13.5 ms 的开销所致。据此我们把单帧算法预算重新核定为约 17 ms，并据此约束后续所有算法的复杂度。软件按相机、光度、检测、几何、估计、控制、通信、执行等模块分层，资源严格逆序释放，并加入了内存漂移监测（长跑十分钟漂移仅约 1%），保证脚本可反复重启而不漏资源。

### 3.2 循线视觉算法（核心）

循线算法是视觉端最具技术深度的部分,目标是从低分辨率灰度图中稳定地估计出车体相对黑线的横向偏差与航向误差,整条流水线被严格压缩在算法预算内。

**光度自适应二值化。** 启动期阻塞采集 30 帧，每帧用图像模块原生的最大类间方差法（Otsu）求阈值并统计背景均值与标准差，取均值融合得到初始阈值；运行期每秒监测ROI区均值，漂移超限即触发 30 帧重标定并热替换阈值。这样无论环境光强弱、明暗，都能让黑线稳定地被分割为前景。

**多扫描带段查找与时空先验选段。** 我们不对整幅图做处理，而是在ROI区内均匀布置 5 条水平扫描带，由远及近分别承担曲率前馈、切线估计与横向偏差主源的角色。早期版本用"全列加权质心"求黑线中心，但只要视野里出现另一块黑（碎屑、走线、阴影），质心就会被按质量加权拉偏，控制律据此跟踪相当于直接撞过去。为此我们重构为**段查找选段**：先对每条带的列向求和找出所有连续的前景段（允许桥接窄洞以抗噪声），再用质量与宽度的几何硬约束筛掉干扰段，最后按"时域先验（与上一帧本带中心最近）→空间先验（与相邻带刚选中的中心最近）→质量兜底"的优先级择优。选段沿近带向远带传播，因为近带是横向偏差的最可信来源。这套方案把抗干扰能力从"先污染再剔除"提前到了"选段时就排除"，五条带总耗时不到 0.4 ms。

**逆透视映射与路径拟合。** 拿到每条带的中心像素坐标后，用逆透视映射（IPM）的单应矩阵把它们换算到地面坐标系。这里我们走了一条务实的捷径：检测每帧只产出 5 个候选点，无需构建整幅图的查找表，直接做 9 元素单应矩阵展开，每帧 5 个点仅约 0.05 ms。地面坐标点送入路径拟合：利用圆环半径约 40 cm 的几何先验，优先用 RANSAC 圆弧拟合（5 点枚举所有三元组、以半径先验筛选、统计内点择优），失败时退化为最小二乘直线拟合。由此解算出横向偏差与航向误差,并对二者各自做 EMA 滤波与符号防抖——单帧反号被强制吸收,连续多帧才接受新基线,避免了控制律被瞬时跳变误导。整条几何链路加起来不足 0.5 ms，丝毫不挤压预算。

**循线控制律。** 控制律由曲率前馈、横向偏差与航向误差的比例反馈、以及限幅与变化率限制组成，输出纵向速度与角速度。考虑到赛道模式下线质量波动会引起反复加减速、干扰瞄准与姿态稳定，我们最终采用"匀速优先"策略，全程保持标称速度，把速度调度的复杂度让位给整体稳定性。

### 3.3 激光瞄准云台闭环

瞄准子系统由云台双舵机直驱，相机随云台运动，因此像素误差是相对当前指向测量的。基于这一点，我们采用了**随动增量式控制律**：把正比于像素误差的角度增量逐拍累加到舵机绝对角度上，这一累加本身就构成单重积分而自然消除静差，无需再设独立积分项；微分项则提供阻尼以抑制超调与检测噪声。像素误差先经 One Euro 滤波平滑再进入控制。

这套控制律是迭代修正的结果。早期版本名为"增量式"实为"位置式 PID 再积分"，叠加约 20 帧每秒视觉与约 40 Hz 控制器的不同步重复驱动，导致约 1 Hz 的极限环摆动，单纯调增益无法消除——这是典型的结构性问题而非参数问题。修复时我们既改正了控制律结构，又引入了**新帧门控**：只有当视觉时间戳前进时才推进一次闭环，杜绝了对同一帧的重复积分。此外，云台俯仰还叠加了基于车身俯仰角的 IMU 前馈：车体一旦前后俯仰，前馈项立即反向补偿，让激光在车身姿态扰动下仍指向靶心，这正是把惯性数据 Y 分直达视觉端的价值所在。目标丢失超过约 2 秒则云台自动回中。

## 四、开发历程与踩坑经验

整个项目按阶段推进——主控从引脚分配、姿态与通信、电机编码器、平衡控制一路到赛道模式；视觉从摄像头采集、光度标定、逆透视、串口链路、闭环循迹一路到瞄准云台与流程对齐。开发过程中积累了大量极具参考价值的踩坑经验，这里择要复盘。

**SDK 多 pad 引脚代码生成体系性缺陷。** 主控 SDK 的图形化配置工具在为"多 pad 绑定"引脚生成 GPIO 初始化代码时存在体系性 bug：它会把引脚解析到一个在所封装上根本不存在的端口，导致链接失败。我们用四种写法逐一尝试均告失败，最终读穿其 JavaScript 代码生成模板，定位到根因是工具在 GPIO 路径下把外设名与接口名拼成了"undefined.undefined"字符串、永远查不到正确的 pad 索引。结论是这条路径无语法层面的解法，只能彻底绕开——放弃用配置工具管理 GPIO，改由板级支持层用引脚控制宏与位常量手工初始化。这件事的教训是：不能把"数据手册上写着某引脚支持某外设"等同于"工具链能在该引脚上正确生成代码"，新增任何外设都应先单独跑一遍代码生成再写驱动。

**两次性质不同的栈溢出。** 第一次是浮点格式化导致的：Cortex-M0+ 默认栈仅 256 字节，而标准库的浮点 printf 单次栈帧就需两三百字节，进入主循环后第一次打印含浮点的心跳即写穿栈、触发硬件错误。修复方法是把栈扩到 1 KB，并把所有浮点打印改成乘 100 取整再用整数格式化，附带还省了几 KB 程序空间。第二次发生在功能叠加后，栈峰值静默覆盖了相邻的全局安全状态变量，导致状态机异常跳变；这次把栈扩到 2 KB，并用前述 canary 魔数主动检测覆盖。两次都印证了嵌入式项目中应完全禁用浮点格式化、并对关键全局状态做完整性保护。

**中断向量命名与编码器雪崩。** 手转右轮会导致整机宕机，排查后发现根因是把 GPIO 中断服务函数命名错了——该器件把多个 GPIO 端口的中断合并到一个共享向量入口，写成传统的单端口入口名只会生成一个永远不会被链接进向量表的普通函数符号，边沿事件触发后跳进默认死循环。这与传统单片机"每外设一个向量"的习惯截然不同。此外，更换为高线数编码器后，右轮软件解码的中断频率在中速即逼近雪崩兜底阈值，我们把右轮解码从四倍频降为两倍频，把边沿率压到阈值的三分之一以内，保住了平衡环的速度反馈。

**视觉端的数值库陷阱。** CanMV 的数值计算库存在多个反直觉行为：按轴求和会静默退化为全体求和、布尔掩码求和会把真值当 255 而非 1、一维加权质心会被广播成一个固定大数。这些都曾让黑线中心计算出离谱的结果，进而让屏幕绘制进入慢路径、把帧率从 30 拖到个位数。我们的应对是关键步骤一律改用手工逐行累加或一次性字节解码,并在每一步加越界与形状的防御性校验。共享内存视图与图像模块的缓存不一致、调试叠加层的性能回退等问题，也都通过改走字节扫描、独立刷新频率等手段逐一化解。

这些踩坑虽然耗时，但都被系统地记录在阶段任务日志中，形成了可复用的经验资产，其长期价值往往超过代码本身。

## 五、总结与展望

本项目以双控制器异构架构完整实现了自平衡瞄准小车的核心功能链：主控侧建成了面向无 FPU 平台高度整数化的多速率级联平衡系统，从单环演进到稳健的两级串级加航向差速闭环，并以赛道状态机统筹自立、循线、判圈与停车的完整流程；视觉侧建成了一条压缩在十余毫秒预算内、却具备光度自适应与多重抗干扰能力的循线流水线，以及随动增量式加 IMU 前馈的激光瞄准云台；两者通过带 CRC 的双向链路与相位闸门机制实现了清晰而稳健的协同。

回顾全程，项目的竞争力主要来自三个层面：一是**工程严谨性**，引脚与协议均有唯一真源、数十次迭代可追溯、踩坑全程归档；二是**架构合理性**，算力按"看世界"与"管车体"解耦，惯性数据 Y 分直达兼顾了双方需求；三是**技术创新性**，无 FPU 整数化平衡架构、sigma-delta 抖动死区、扫描带时空先验选段、随动增量式瞄准与相位闸门软同步，都是针对具体瓶颈给出的有效方案。

后续工作将集中在三方面：其一，完成各阶段验收表中尚待装车回填的实测指标，尤其是循线脱线率、激光波动幅度与整圈时间；其二，把右轮软件解码迁移到定时器捕获模式以释放 CPU、并探索基于倒立摆模型的参数自整定以替代手工试凑；其三，向发挥项进阶——连续两周行进、激光波动压到 2 cm 以内、以及在保证稳定的前提下的速度优化。这些方向已在现有架构上预留了清晰的接口，具备良好的延续性。
