# 智能平衡车项目设计报告

## 1. 概述

本项目面向智能车竞赛与实车验证场景，设计并实现了一套基于 MSPM0G3507 主控和 K230 视觉模块的智能平衡车系统。车辆需要在自身不稳定的两轮结构上完成姿态平衡、速度控制、循迹行驶、状态保护和比赛流程执行，因此系统设计的重点并不是单个算法的堆叠，而是让感知、控制和安全机制能够在真实车体上稳定协同。

整车采用“主控实时闭环 + 视觉智能导航”的分工方式：MSPM0G3507 负责姿态采集、电机驱动、编码器反馈、平衡控制、安全保护以及赛道流程管理；K230 负责采集赛道图像、识别黑色引导线、估计车辆相对赛道的位置偏差与方向偏差，并向主控发送运动建议。两者之间通过自定义串口协议交换状态和指令，使视觉模块能够参与决策，同时不直接接管电机输出。

项目的创新性主要体现在四个方面：一是以多环控制和状态机组织平衡车的实时控制，使车辆具备从自起立到循迹行驶的完整行为链；二是针对 K230 平台实现了轻量化视觉循迹算法，在算力和采帧限制下保证可运行、可调试；三是设计了带校验和超时保护的 K230-MCU 通信协议，提高跨模块协作可靠性；四是围绕栈溢出、采帧瓶颈、调参分阶段等真实问题进行工程化迭代，使作品更接近可参赛、可复现的系统，而不是停留在单点功能演示。

## 2. 主控 MCU 设计

主控 MCU 是整车的实时控制中枢。对于平衡车而言，任何上层感知和路径规划都必须建立在车体稳定的基础上，因此主控侧的首要任务是维持姿态平衡，并把来自视觉模块的运动意图转化为底盘可执行的闭环目标。项目没有让 K230 直接控制电机，而是由 MCU 统一处理姿态、速度、差速、航向、安全和比赛阶段。这样的设计使整车的关键实时环路留在低延迟、确定性更强的控制器中，同时让视觉模块专注于感知和高层指令生成。

从软件结构看，MCU 程序按照底层驱动、中间算法和应用控制三层组织。底层驱动负责电机、电池、电平输入输出、串口、系统节拍和编码器等硬件访问，尽量屏蔽寄存器和外设细节；中间层负责姿态解析、PID 控制、参数管理、协议编解码和调试输出；应用层负责平衡控制、安全状态、循迹流程、蜂鸣提示和遥测。分层的意义不只是代码整洁，更重要的是便于定位问题：当车辆表现异常时，可以较快判断问题来自传感器输入、执行器输出、控制参数、通信链路还是状态逻辑，减少实车调试中的盲目试错。

平衡控制系统采用多环结构。内层姿态环以较高频率工作，直接根据车体俯仰角和角速度计算电机输出，是保持车辆不倒的核心；外层速度环根据编码器反馈计算目标倾角，使车辆能够在平衡的前提下前进、减速或制动；差速环根据左右轮速度差修正转向输出，使车体能够响应角速度指令；航向辅助则用于在特定阶段抑制无意偏转或增强方向稳定性。多环设计的优点在于把“站稳”和“走对”分开处理：内环只关心车体是否稳定，外环再逐步施加速度和方向目标，避免视觉指令直接冲击电机造成失稳。

在时间安排上，系统采用不同频率执行不同任务。姿态相关控制保持较高更新率，确保车体倾倒趋势能被及时抑制；速度、差速和航向等外环以较低频率更新，既满足运动控制需要，也避免编码器噪声被过度放大；通信接收和状态检查在主循环中持续进行，保证 K230 指令、心跳和调试命令能被及时处理。这样的多速率安排符合平衡车的物理特性：姿态变化快，必须快速响应；路径和速度变化相对慢，可以通过较平滑的目标更新来提高稳定性。

来自 K230 的目标速度和角速度不会被 MCU 直接解释为电机 PWM。视觉模块只提供“希望车辆怎样走”的建议，MCU 则根据当前安全状态、车体姿态、编码器速度和比赛阶段判断是否接受以及如何执行。目标速度会被转换为速度环输入，目标角速度会被转换为差速或航向相关目标，最终仍由平衡控制闭环输出左右轮控制量。这样处理后，即使视觉识别出现瞬时波动，车辆也不会立即产生剧烈电机动作，而是经过限幅、滤波、闭环反馈和安全状态约束后再作用到底盘。

比赛流程控制是 MCU 设计中另一个重要部分。系统将车辆行为划分为待机、自起立、稳定等待、循迹行驶、刹停、暂停、最终刹停和完成等阶段。自起立阶段使用专门的控制参数，使车辆先从倒伏或大角度状态恢复到可平衡姿态；稳定等待阶段给传感器、速度估计和控制环留出收敛时间；循迹阶段才允许 K230 的运动指令真正参与行驶；刹停和暂停阶段则用于完成圈数控制或中途节奏控制。通过状态机组织流程，车辆不再只是“能平衡”或“能接收指令”，而是具备完整的竞赛行为逻辑。

这种流程设计还解决了视觉模块与底盘控制之间的权限问题。K230 的识别结果只有在 MCU 确认车辆处于循迹阶段时才会转化为有效运动，否则视觉模块即使检测到赛道，也不能单方面让车移动。对于平衡车这种本体不稳定的平台，这一点非常关键：车辆在刚起立、跌倒恢复、低电压或通信异常时，视觉结果并不一定代表可以安全行驶。由 MCU 统一裁决运动权限，可以避免“视觉看见了路就让车跑”的简单方案带来的风险。

安全机制贯穿主控设计。系统在开机后设置检查阶段，避免传感器尚未稳定或电池采样尚未可靠时立即进入控制；跌倒保护通过姿态角持续越界判断车辆是否已经失控，并及时刹车或关闭输出；低电压保护先进入限功率警告，再在严重低电压时停止驱动，减少电池和电机风险；通信超时机制在一段时间没有收到 K230 有效指令后自动清零运动目标，防止串口中断或视觉程序卡死后车辆继续按旧指令行驶；PWM 限幅则为调参和异常情况下的输出提供最后边界。

值得强调的是，安全逻辑不是附加功能，而是实车开发能否持续推进的基础。平衡车调参往往需要反复上电、扶车、放手、观察和修改参数，如果没有跌倒保护、输出限幅和通信超时，每一次失败都可能损坏机械结构或让调试者失去对系统的判断。项目中还加入了状态保护和异常检测思路，用于发现内存破坏或状态异常。这些措施虽然不直接提升理论控制性能，却显著提高了整车的可调试性和可靠性。

从竞争性角度看，MCU 侧的价值在于把一个容易失控的双轮平台整理成可被视觉模块安全驱动的底盘。相比只实现简单平衡或只实现遥控前进的方案，本项目通过多环控制、分阶段参数、比赛状态机和安全机制形成了完整控制骨架。视觉导航的复杂性被隔离在 K230 侧，车体稳定性的责任则始终由 MCU 承担，两部分边界清晰、互相约束，使系统既有扩展空间，又不牺牲底层实时性。

## 3. K230 视觉与通信设计

K230 在系统中承担高层感知和导航任务。它不直接输出电机控制量，而是根据摄像头图像判断赛道相对于车体的位置和方向，再生成目标速度、目标角速度等运动建议。这样做符合 K230 的优势：它更适合处理图像、执行几何估计和运行调试界面，而不适合承担毫秒级电机闭环。项目将 K230 定位为“看路并给出建议”的视觉导航单元，使主控和视觉模块形成互补关系。

视觉循迹的基本流程从图像采集开始。前向摄像头获取赛道灰度图后，程序先进行光照适应，估计当前画面中黑线与地面的亮度差异。由于比赛现场光照、地面反光和摄像头曝光都可能变化，固定阈值很难长期可靠。系统采用自适应阈值和兜底策略：当画面统计特征稳定时使用较可信的自动阈值；当亮度波动过大或估计不可靠时，保守调整阈值，避免把整片背景误识别为赛道线。这个环节看似简单，却直接决定后续几何估计是否可信。

在目标提取上，项目采用轻量化分带扫描，而不是运行复杂的深度学习模型。图像被按距离方向划分为若干条横向扫描带，每个扫描带中统计黑色像素分布，寻找可能属于赛道线的连续区域。随后系统会结合上一次检测结果、相邻扫描带的空间连续性和当前候选区域的像素质量，选择最可能的线段中心。这样做的优点是计算量小、解释性强、调试直观，尤其适合 K230 上受限的实时运行环境。对于循迹任务而言，只要赛道线对比度足够，轻量算法往往比复杂模型更容易做到稳定、低延迟和可控。

得到图像中的线段中心后，K230 还需要把“图像位置”转化为“地面位置”。由于摄像头存在俯仰角和透视畸变，图像中同样的像素偏移在近处和远处对应的实际距离并不相同。项目通过地面映射将若干关键扫描带的中心点投影到车体前方的地面坐标系中，再根据这些点估计赛道的几何形状。当点分布符合弯道特征时，系统尝试用圆弧模型拟合路径；当弯道信息不足或质量较低时，则退回到直线模型，仍然给出横向偏差和航向偏差。这种“圆弧优先、直线兜底”的策略使车辆既能处理弯道，又不会在信息不足时完全失去控制依据。

视觉估计并不只输出一个“向左”或“向右”的粗略判断，而是尽量给出可连续控制的误差信息。横向偏差表示车辆相对于赛道中心偏左或偏右，航向偏差表示车头方向与赛道切线方向的夹角，曲率信息则反映前方路径弯曲程度。K230 根据这些量计算角速度建议：横向偏差用于把车拉回赛道，航向偏差用于让车头提前对准路径，曲率项则可作为弯道前馈，使车辆在进入弯道前就产生转向趋势。与只根据画面中心偏差转向的方案相比，这种几何化处理能更好地区分“车身偏了”和“路本身在转弯”两种情况。

为了避免视觉噪声直接变成抖动指令，系统还引入了质量评价和输出平滑。质量评价综合考虑黑线像素数量、连续性、几何拟合可信度和路径模型合理性。当图像质量较高时，车辆可以按正常速度行驶；当质量下降时，控制输出会变得保守，必要时降低速度或保持上一段可信结果，而不是对瞬时误检做剧烈响应。输出端还会进行限幅和变化率限制，使角速度指令更加平滑。平衡车本身对突变输入敏感，这一层处理相当于在视觉和底盘之间增加了缓冲。

K230 侧还保留了第二路视觉能力，用于瞄准或目标识别。第二路摄像头主要面向红色目标、瓶盖或其他任务目标，通过颜色阈值、面积、密度和形状比例筛选目标区域，并配合舵机云台进行指向控制。虽然这部分不是循迹闭环的主线，但它说明系统架构并未把 K230 限定为单一摄像头循迹，而是为后续扩展目标识别、瞄准执行、无线调试和多任务竞赛流程留下了空间。

视觉性能优化是 K230 设计中的关键经验。早期调试中发现，单纯降低图像分辨率或简化算法并不能无限提升帧率，瓶颈不只在算法本身，也来自运行时机制和全局解释器锁。尤其是单线程反复调用 `snapshot` 时，采帧过程存在实际帧率上限，显示叠加、图像扫描和串口处理又会进一步挤占时间。项目后续采用双进程思路，把采帧和处理尽量分离，让摄像头获取与算法处理减少互相等待，从而绕过单线程采帧调用带来的上限。配合减少显示覆盖开销、使用字节级扫描和控制调试刷新频率，视觉链路变得更加稳定。

这类优化的意义不在于追求纸面算力指标，而在于让算法真正跑在目标硬件上。对于竞赛小车，视觉算法只有在固定周期内稳定输出，才有资格进入控制闭环。复杂模型如果导致帧率不稳定或延迟过大，反而会降低车辆表现。项目最终选择的轻量分带扫描、地面映射和几何估计组合，正是基于这一判断：在 K230 的平台约束下，优先保证可解释、可调试、可实时运行，再逐步提高识别鲁棒性。

K230 与 MCU 的通信协议是整车协作的纽带。物理链路采用串口连接，波特率选择兼顾可靠性和实现难度。IMU 数据采用分流思路，使姿态传感器的输出同时被 MCU 和 K230 获取：MCU 用于实时平衡控制，K230 可用于视觉辅助和调试分析。这样避免了让 MCU 先接收姿态数据再转发给 K230 的额外负担，也减少了主控串口带宽压力。MCU 与 K230 之间只传递车辆状态和运动指令，链路职责更加清晰。

协议帧采用固定帧头、长度、命令、载荷、校验和固定帧尾的结构。帧头和帧尾用于在连续字节流中快速寻找边界，长度字段用于判断载荷范围，命令字段用于区分车辆状态、运动指令、心跳、参数注入和文本调试等不同类型，校验字段用于发现错帧、丢字节或串扰导致的数据损坏。相比直接发送裸数据或字符串命令，这种二进制帧格式解析开销小、扩展方便、抗错能力强，也更适合在车辆运动过程中长期运行。

通信内容按照“状态上报”和“运动建议”划分。MCU 周期性上报平均轮速、安全状态、电池电压、当前比赛阶段和圈数等信息，使 K230 能知道车辆是否已经进入循迹阶段、是否处于低电压或异常状态，以及当前流程进度。K230 则向 MCU 下发目标速度、目标角速度和模式信息，同时发送心跳保持在线状态。调试期间还可以通过文本命令或参数注入调整控制参数，减少反复烧录程序的成本。这样的协议既服务于正式运行，也服务于开发调试。

容错机制是协议设计的核心之一。接收端在解析时会检查帧头、长度、校验和帧尾，不符合规则的数据直接丢弃，避免错位字节被误解释为有效指令。心跳和超时保护用于处理 K230 程序卡死、串口断开或线缆接触不良等情况：一旦 MCU 在规定时间内没有收到有效指令，就把运动目标清零，让车辆回到受控状态。K230 侧也会根据 MCU 上报的比赛阶段决定是否发送非零运动指令，避免双方状态不同步时出现危险动作。

从系统竞争性看，通信协议体现了本项目与简单拼接式方案的差别。视觉模块并不是“看见黑线就控制电机”，而是通过明确协议把目标、状态和权限分开；MCU 也不是被动执行串口数据，而是在安全状态和控制闭环内解释这些数据。这样的结构对平衡车尤其重要，因为平衡车的底盘稳定性本身就是一个持续任务，任何高层指令都必须服从底层姿态控制。项目采用“视觉建议 + MCU 闭环执行”的模式，使整车同时具备智能感知和安全执行能力。

## 4. 开发历程与踩坑经验

项目开发过程并不是一次性完成，而是在硬件接入、姿态采集、电机控制、平衡调参、视觉循迹和跨模块通信中不断迭代。比较有价值的经验并非普通引脚分配或模块连接，而是那些直接改变系统设计方式的问题：栈溢出提醒我们嵌入式调试也要受资源约束；平衡控制调参说明不同运动阶段需要不同策略；K230 视觉性能瓶颈则推动了采帧与处理方式的重构。这些问题共同把系统从“能跑的代码”推向“能在实车上持续调试的作品”。

首先是栈溢出与嵌入式稳定性问题。早期为了观察姿态、速度和控制输出，调试信息中使用了较重的格式化输出，尤其是浮点格式化会明显增加栈占用。在资源有限的 MCU 上，这类输出可能不会立刻表现为清晰的编译错误，而是在运行中引发状态破坏、异常跳转或看似随机的控制失效。由于故障表面上可能像是 PID 参数错误、传感器数据异常或串口干扰，定位难度较高。项目后续减少了重型输出，更多使用整数化、缩放后的日志格式，并结合栈空间调整和状态保护手段提升可靠性。

这一教训说明，嵌入式系统中“能打印出来”不等于“适合长期打印”。调试链路本身也会消耗 CPU 时间、串口带宽和栈空间，如果调试方式破坏了实时性，就会让观测结果失真。对于平衡车这样的实时控制系统，日志应服务于定位问题，而不能成为新的不稳定来源。项目中将关键遥测控制在必要范围内，并用更轻量的格式输出，既保留了观察能力，也降低了对控制环的影响。

第二类经验来自平衡控制调参。自起立阶段和平衡行驶阶段虽然都依赖姿态控制，但控制目标并不相同。自起立时车辆角度大、速度估计不稳定，电机需要把车体拉回平衡区间，此时如果沿用行驶阶段的参数，尤其是使用较强的积分作用，容易出现积分累积和 PWM 饱和，导致车体冲过平衡点或持续抖动。进入稳定行驶后，系统又需要速度环和差速环参与，才能响应视觉模块的前进和转向需求。

因此项目采用分阶段控制：自起立阶段使用更适合大角度恢复的参数，并抑制不必要的积分累积；车辆进入可控姿态并稳定等待后，再切换到平衡行驶参数，让速度和转向目标逐步参与。这个过程体现了实车控制中的一个重要原则：不要试图用一套参数覆盖所有物理阶段。对平衡车而言，站起来、站稳、开始走、沿线转弯是不同任务，需要在状态机中明确边界。分阶段设计比单纯调大或调小某个 PID 参数更可靠，也更便于复盘问题。

第三类经验是 K230 视觉性能瓶颈。最初容易把帧率不足归因于算法复杂度，于是尝试降低分辨率、减少图像处理步骤或关闭部分显示内容。但实际调试表明，瓶颈还与平台采帧机制、运行时调度和全局解释器锁有关。单线程中采帧、处理、显示、通信全部串行执行时，`snapshot` 调用会形成明显上限，即使算法已经很轻，整体帧率也难以继续提升。后续通过双进程采帧与处理分离，减少采帧等待对算法链路的阻塞，使系统绕过了单线程调用的限制。

同时，项目还对视觉链路中的一些细节进行了取舍。显示叠加虽然有利于观察，但如果每帧绘制过多元素，会直接拖慢主循环；数组运算在平台上的行为也并非总是符合桌面 Python 经验，某些看似简洁的写法反而会引入异常结果或性能问题。最终系统更多采用可控的字节级扫描、分频刷新调试画面和轻量几何估计。这个过程说明，嵌入式视觉优化不是单纯追求算法“高级”，而是在平台限制、实时性、可解释性和调试成本之间做平衡。

除上述三点外，串口校验、编码器计数、视觉阈值和电池采样等问题也在开发中不断暴露。它们共同指向同一个结论：鲁棒性不是最后添加的功能，而是在每一次失败后逐步沉淀出来的系统能力。项目最终形成的状态机、协议校验、超时保护、轻量日志和分阶段控制，都是从实际问题中反推出来的设计选择。这也是本项目报告中值得强调的工程价值。

## 5. 总结与展望

本项目完成了一套以 MSPM0G3507 为实时控制核心、以 K230 为视觉感知核心的智能平衡车系统。MCU 侧实现了姿态采集、电机闭环、平衡控制、速度与差速调节、安全保护和比赛流程管理；K230 侧实现了视觉循迹、路径几何估计、运动指令生成、目标识别扩展和调试通信；两者通过带校验和心跳保护的串口协议协同工作。系统不是把多个功能简单相加，而是围绕平衡车的实时性和安全性建立了清晰分工。

从成果看，项目的竞争力主要来自整体架构。平衡控制保证车辆具备稳定运动基础，视觉算法提供赛道理解能力，通信协议把两者连接成可控闭环，安全机制则保障调试和比赛过程中的可恢复性。与只依赖视觉直接控制电机的方案相比，本项目更适合本体不稳定的两轮平台；与只实现平衡或遥控的方案相比，本项目又具备自主循迹和流程执行能力。其创新点并不依赖某一个孤立算法，而体现在软硬件分工、实时闭环、轻量视觉、状态管理和工程容错的综合设计。

后续仍有进一步优化空间。K230 视觉管线可以继续完善双进程采帧和处理调度，提高复杂场景下的帧率稳定性；赛道识别可以增强对交叉、断线、强反光、局部遮挡等情况的处理；参数整定可以引入更系统的日志回放和离线分析工具，减少实车试错成本；瞄准识别、无线调试和自动标定也可以纳入完整比赛流程，使 K230 的扩展能力得到更充分发挥。总体而言，本项目已经构建起较完整的智能平衡车技术框架，后续优化可以在现有分层架构和通信协议基础上持续推进。
